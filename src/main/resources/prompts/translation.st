Translate the following text to {targetLanguage}.

Return the result as a JSON object with these exact fields:
- originalText: the input text
- translatedText: the translated text
- sourceLanguage: the detected source language
- targetLanguage: {targetLanguage}
- confidenceScore: a number between 0.0 and 1.0 representing translation confidence

Text to translate:
{text}
